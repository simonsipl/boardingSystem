class OutsideInsideClient {
    constructor(requestClient, type) {
        this.requestClient = requestClient;
        this.type = config;
    }

    isSatisfiedBy(type) {
        return type === 'outsideinside';
    }

    sort() {



    }
}
